export const sidebar = useLocalStorage<string | null>('sidebar', null)

export const locale = useLocalStorage<string | null>('locale', null)

export const token = useLocalStorage<string | null>('token', null)